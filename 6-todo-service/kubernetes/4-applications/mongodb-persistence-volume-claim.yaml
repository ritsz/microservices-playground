apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb-pvc
  namespace: applications
spec:
  resources:
    requests:
      storage: 2Gi 
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
